import React from 'react';
import styled from 'styled-components';

const Home = () => (
  <div>
    <Header>Coronavirus Chile</Header>
  </div>
);

export default Home;

const Header = styled.h1`
  color: blue;
`;
